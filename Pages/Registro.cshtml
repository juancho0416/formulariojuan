@page
@model RegistroModel
@{
    ViewData["Title"] = "Registro";
}
<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/First"><i class="icon icon-home"></i></a></li>
    <li class="breadcrumb-item"><a href="/index">Inicio</a></li>
    <li class="breadcrumb-item active" aria-current="page"><a href="/Index">Login</a></li>
</ol>

<div class="row justify-content-center">
    <div class="col-md-6">
        <h1>Registro</h1>
        <hr class="red">
        </hr>
        <form method="post" novalidate>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Input.Name" class="form-label">Nombre</label>
                <input asp-for="Input.Name" class="form-control" type="text" placeholder="Ingrese su nombre" />
                <span asp-validation-for="Input.Name" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Input.Apellido" class="form-label">Apellido</label>
                <input asp-for="Input.Apellido" class="form-control" type="text" placeholder="Ingrese su apellido" />
                <span asp-validation-for="Input.Apellido" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Input.Email" class="form-label">Email</label>
                <input asp-for="Input.Email" class="form-control" type="text" placeholder="Ingrese su correo" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Input.Password" class="form-label">Contraseña</label>
                <input asp-for="Input.Password" class="form-control" type="password"
                    placeholder="Ingrese su contraseña" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary pull-right">Registrarse</button>
        </form>
        @if (!ModelState.IsValid)
        {
            <div class="mt-3 alert @(ModelState.IsValid ? "alert-success" : "alert-danger")">
                @if (ModelState.IsValid)
                {
                    <div> Login/registro válido</div>
                    @if (TempData["Mensaje"] != null)
                    {
                        <div class="mt-2">@TempData["Mensaje"]</div>
                    }
                }
                else
                {
                    <div>Datos invalidos,corrijalos porfavor</div>
                }
            </div>

        }
        <div>
            <a asp-page="/Index" class="btn btn-danger">Reintentar</a>

        </div>

        @section Scripts {
            <partial name="_ValidationScriptsPartial" />
        }
    </div>